project: source-wand
hooks:
  before_all: null
  before_each: null
  after_each: null
  after_all: null
origin:
  git: <url to your project's repository>
  reference: <reference to checkout>
destination_template:
  git: <where to replicate your project>
  reference: <reference to push>
config: null
